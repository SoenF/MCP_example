[project]
name = "mcp-example"
version = "0.1.0"
description = "Claude & OpenRouter agents connected to data.gouv.fr via MCP"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.49.0",
    "mcp>=1.3.0",
    "openai>=1.58.0",
    "python-dotenv>=1.0.0",
]
